{{>header}}

<main>

  <div class="content-container">
	<section class="stashes", id="id-stashes">

      		{{#each stashes}}
        		{{>stash}}
      		{{/each}}

  	</section>
  </div>

  {{>sidebarStash}}

  {{>modalStash}}

</main>
