{{>header}}

<main>

  <div class="content-container">
	<section class="posts">

      		{{#each posts}}
        		{{> post}}
      		{{/each}}

	</section>
  </div>

  {{>sidebarPost}}

  {{>modalPost}}

</main>
