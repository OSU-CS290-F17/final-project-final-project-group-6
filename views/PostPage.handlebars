{{>header}}

<main>

  <div class="content-container" id="id-content-container">
	<section class="posts", id="id-posts">

      		{{#each posts}}
        		{{> post}}
      		{{/each}}

	</section>
  </div>

  {{>sidebarPost}}

  {{>modalPost}}

</main>
