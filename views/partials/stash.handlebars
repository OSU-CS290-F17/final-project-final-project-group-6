<div class="stash" id="id-stash" data-stash-id="{{stashId}}" data-topic="{{topic}}">
    <div class="stash-box">
      <div class="stash-title-box">
        <a href="{{linkURL}}" class="stash-title">{{topic}}</a><br>
        <div class="stash-desc">{{text}}</div>
      </div>
    </div>
</div>
