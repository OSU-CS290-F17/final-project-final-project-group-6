<div class="stash" data-stash-id="{{stashId}}" data-topic="{{topic}}">
    <div class="stash-box">
      <div class="stash-title-box">
        <a href="{{linkURL}}" class="stash-title">{{topic}}</a> <span class="stash-desc">{{text}}</span><br>
      </div>
    </div>
</div>
